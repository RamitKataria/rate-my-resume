{
  "name": "resume-game-server",
  "scripts": {
    "start": "node ./server",
    "dev": "nodemon ./server",
    "build-client": "cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "debug": "~3.2.7",
    "dotenv": "^6.1.0",
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "express-openid-connect": "^2.17.0",
    "http-errors": "~1.8.1",
    "mongoose": "^7.6.2",
    "morgan": "~1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
